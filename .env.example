# API-Football (plan free = saisons 2022-2024 uniquement, données obsolètes)
# Créer un compte sur https://api-football.com/ - 100 req/jour gratuit
# API_FOOTBALL_KEY=xxx

# 1 = appeler API-Football (blessures, standings, fixtures) | 0 = désactivé (scraping seul)
# ENABLE_API_FOOTBALL=0

# Scraping - complète les blessures via Transfermarkt, L'Equipe, RSS, etc.
# 1 = activé (défaut) | 0 = désactivé
# ENABLE_SCRAPED_INJURIES=1

# Hiérarchie des sources pour statuts (blessés, incertains). Ordre = priorité en cas de conflit.
# Valeur : rss,sofascore,transfermarkt,mpg (réactivité : RSS > SofaScore > Transfermarkt > MPG)
# STATUS_SOURCE_PRIORITY=rss,sofascore,transfermarkt,mpg

# En conflit "blessé/incertain" (autre source) vs "apte" (MPG) : faire confiance à MPG et retirer des listes ?
# 1 ou true = oui | 0 ou false = non (défaut, plus prudent)
# TRUST_MPG_APTE_WHEN_CONFLICT=0
